<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android75" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android75" >

        <activity
            android:name=".PlayReplay"
            android:exported="false"
            android:label="@string/title_activity_play_replay"
            android:parentActivityName=".replaylist"
            android:theme="@style/Theme.Android75.NoActionBar" >
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.example.android75.replaylist" />
    </activity>

        <activity
            android:name=".replaylist"
            android:exported="false"
            android:label="@string/title_activity_replaylist"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Android75.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.android75.MainActivity" />
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>